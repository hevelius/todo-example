# Tasks
1. Create the types Task, hook useTasks (state + persistance localStorage)
2. Create components TaskList, TaskItem, AddTaskForm with related test
3. Adds filter for completed / to do / all above TaskList